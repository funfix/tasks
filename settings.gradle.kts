rootProject.name = "tasks"

include("tasks-core", "tasks-kotlin", "tasks-java-tests")

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}
